import 'package:flutter/material.dart';

/// Paleta central de colores de la aplicación.
/// Todos los colores del diseño deben referenciarse desde aquí.
class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF73A6AD);
  static const Color primaryDark = Color(0xFF1C3144);

  // Secondary
  static const Color secondary = Color(0xFF9B97B2);
  static const Color secondaryLight = Color(0xFFD8A7CA);
  static const Color secondaryDark = Color(0xFF0B132B);

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Background & Surface
  static const Color background = Color(0xFFF0F8FF);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);

}
